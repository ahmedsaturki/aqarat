# سجل التغييرات

## [Unreleased] — 2026-08-17

### تجربة المشغّل والاعتمادية

أضيفت حماية Origin و`Sec-Fetch-Site` لتسجيل دخول لوحة التحكم عند توفر بيانات المتصفح، مع endpoint `dashboard/logout` idempotent يمسح جلسة `HttpOnly` الآمنة، وزر تسجيل خروج واضح يعيد المشغّل إلى شاشة الدخول. كما أضيفت اختبارات مباشرة لعقد الجلسة ومسحها. تم دمج ذلك عبر PR #23 ونجح verify-production على commit `67b2ac9`.

تم تحسين لوحة التشغيل بواجهة تنقل دلالية، ورابط لتجاوز التنقل، وتسميات نماذج صديقة لقارئات الشاشة، ومناطق حالة حية، وتركيز مرئي للوحة المفاتيح، ودعم `prefers-reduced-motion`. كما أصبح تسجيل الدخول يعرض حالة تحقق محدودة، ويمسح حقل كلمة المرور بعد النجاح، ويعرض خطأ اتصال عامًا عند تعذر الوصول إلى الخادم. أضيفت طبقة `timedFetch` مشتركة لاستخدام مهلة outbound bounded في قراءات بيانات لوحة التشغيل من Supabase، مع اختبارات مباشرة للإشارة والإلغاء.

تم دمج الدفعات السابقة عبر PR #15 وما تلاه من PRs لتحسين الأمن والرصد والأداء. وفي PR #21 نجحت فحوصات `test` و`visual` و`performance` وVercel، ثم تم الدمج إلى `main` على commit `40542c2`. بعد الدمج نجح `npm run check` بعدد 106 اختبارات، كما نجح workflow `verify-production` Run #32029896494 بما في ذلك فحوصات الإنتاج والصحة العميقة.

### الرصد والتشخيص

أضيفت سجلات JSON منظمة لبداية الطلب ونهايته وأخطاء التوجيه، مرتبطة بمعرّف الارتباط، مع قياس زمن الاستجابة وحجم الاستجابة دون تسجيل جسم الطلب أو الرؤوس أو بيانات الاعتماد. كما أصبحت الاستجابات تحمل `x-response-time-ms` و`server-timing` لقياس الأداء بصورة آمنة.

أضيفت `safeErrorMessage` لحماية سجلات الأخطاء من رسائل المزود غير الموثوقة؛ فهي تخفي أسرار query، وBearer tokens، وكلمات المرور، وعناوين البريد الإلكتروني، وتقتطع الرسالة إلى حد أقصى 512 حرفًا. أضيفت اختبارات انحدار مباشرة لهذا العقد.

### جودة البيانات

أصبح محرك الإدخال يرفض المدخلات غير النصية والفارغة والرسائل التي تتجاوز 4,000 حرف، ويتحقق من حدود الأسعار والمساحات وأعداد الغرف وأرقام القطع وأرقام الاتصال. وتبقى القيم الشاذة القابلة للتفسير محفوظة مع تحذيرات واضحة بدل إسقاطها صامتًا، بما يدعم المراجعة البشرية وجودة البيانات.

### لوحة التشغيل

أضيف بحث bounded وUnicode-safe إلى أقسام لوحة التحكم، مع whitelist لحقول البحث، حد 80 حرفًا، encoding آمن لمعاملات Supabase، وزر مسح يعيد pagination إلى البداية. لا يطبق البحث على KPI/overview أو insights التي تعتمد على تجميعات مستقلة. تم دمج ذلك عبر PR #24 ونجح verify-production على commit `70a1930`.

أضيف ترقيم صفحات bounded إلى واجهة بيانات لوحة التشغيل بحد افتراضي 50 صفًا وحد أقصى 100، مع `has_more` و`next_offset` و`returned`. كما أصبحت أخطاء اللوحة عامة وقابلة لإعادة المحاولة وتحمل معرّف الارتباط، وتعرض الواجهة رسالة تشغيلية مفهومة بدل كشف تفاصيل المزود.

### العمال والـ queues

قبل claim jobs في Sheets worker، تُستدعى دالة `requeue_expired_jobs` الموجودة لإعادة jobs ذات leases المنتهية إلى الطابور. فشل maintenance لا يوقف العامل، وتوجد اختبارات runtime تثبت ترتيب الاستدعاءات والتدهور الآمن. تم دمج ذلك عبر PR #25 ونجح verify-production على commit `c2e88f1`.

### Gemini والتحقق الإنتاجي

تم جعل فحص Gemini العميق يستخدم افتراضيًا probe منخفض التكلفة يعتمد على metadata للتحقق من صحة المفتاح، وتوفر النموذج، ودعم `generateContent` دون استهلاك حصة التوليد. يبقى فحص التوليد الكامل خيارًا تشخيصيًا صريحًا عبر `GEMINI_HEALTH_PROBE=generation`، وتظل أخطاء المزود حالات فشل واضحة ولا تُحوّل إلى نجاح. اجتاز الإصلاح Run #53 من workflow `verify-production` على commit `3fcb28d`؛ نجحت مهمتا `unit` و`production`، ونجحت خطوة `Run authenticated deep health check`، بينما تم تخطي مسار غياب السر لأنه غير مطلوب عند توفر السر.

### الاختبارات والتوثيق

أضيفت اختبارات لعقد ترقيم لوحة التشغيل، وأخطاء المزود، وحدود الإدخال، والتحقق من الهاتف، والقيم الشاذة، وقياسات استجابة الموجّه. وتم تحديث `.env.example` و`README.md` لتوثيق حدود التشغيل ومتغيرات التحقق في GitHub Actions. كما اجتاز `production-smoke.sh` النهائي نقاط الصحة العامة، ورفض الصحة العميقة غير الموثقة، ورؤوس الأمان المطلوبة. وراجعت الدفعة جميع استدعاءات `fetch` المتبقية: التكاملات التشغيلية تستخدم `timedFetch`، بينما يحتفظ محول discovery وprobes الصحة/الإصدار/الأداء بحدود متخصصة موثقة لعقود SSRF/streaming والقياس. لا تتضمن هذه الدفعة حذف فهارس أو تغيير أسرار أو migrations إنتاجية.
