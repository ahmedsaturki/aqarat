{
  "name": "aqarat-os",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check:syntax": "find src api scripts test -type f -name '*.mjs' -print0 | xargs -0 -n1 node --check",
    "check": "npm test && npm run check:syntax",
    "start:intake": "node src/runtime/intake-api.mjs",
    "verify:release": "node scripts/verify-release.mjs",
    "verify:backup": "node scripts/verify-backup-readiness.mjs",
    "test:visual": "playwright test",
    "performance:smoke": "node scripts/performance-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
